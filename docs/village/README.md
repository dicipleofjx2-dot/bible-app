# 신바람목장 (목장마을ON)

목장이 **작은 교회**가 되고, 목장들이 모여 **마을**이 된다.
기준 문서: `C:\Users\dicip\Downloads\목장마을ON_앱기획서.md`

홈 화면 🏡 **신바람목장** → `/village`

---

## 먼저 할 일 — 마이그레이션

`supabase/migrations/0085_village_on.sql` 을 **Supabase 대시보드 SQL Editor에서 실행해야**
화면이 살아난다. 실행 전에는 방이 열려도 표가 없어 전부 빈 화면이다.

프로젝트: `bhqbrkeoiyhnmdgvofvy` (데이빗바이블 · 스마트주보와 같은 DB)

---

## 무엇을 새로 만들지 **않았는가**

0065 가 세운 원칙을 그대로 잇는다. 교적이 이미 아는 것은 다시 담지 않았다.

| 사실 | 어디에 있나 |
|---|---|
| 목장 목록 | `org_units (unit_type='cell')` — 스마트주보 교적 |
| **마을** | `org_units (unit_type='district')` + 목장의 `parent_id` |
| 누가 어느 목장 | `members.cell_id` |
| 목자 자격 | `org_units.leader_member_id` · `church_memberships.role` |
| 정기 모임 시간·장소 | `gather_recurrences` |
| 심방 신청 | `cell_visit_requests` (0065) |
| 목장 공지·보고·소통창 | `cell_notices` · `cell_reports` · `cell_messages` (0065·0068) |

**마을 표를 따로 만들지 않은 까닭**: 교적에서 목장을 옮겨도 마을이 안 따라오면
0068 이 겪은 「24건 중 7건이 어긋난」 일이 되풀이된다. 교구를 아직 안 나눈 교회는
`village_id = null` 로 서고 **교회 전체가 한 마을**이 된다(`can_see_village(null)`).

---

## 화면

| 길 | 파일 | 하는 일 |
|---|---|---|
| `/village` | `src/app/village/index.tsx` | 목장 현관 — 건물 그림, 이번 모임, 방 여섯 칸 |
| `/village/chapel` | `chapel.tsx` | 예배당 — 모임 만들기·순서 진행·참석·기록 |
| `/village/share` | `share.tsx` | 소그룹실 — 나눔과 댓글 |
| `/village/nurture` | `nurture.tsx` | 양육실 — 과정 카드, 목장에는 집계만 |
| `/village/care` | `care.tsx` | 돌봄실 — **목자·교역자만** |
| `/village/ministry` | `ministry.tsx` | 사역실 — 카드와 역할 지원 |
| `/village/mission` | `mission.tsx` | 선교실 — 선교지 소식과 중보 |
| `/village/square` | `square.tsx` | 마을 — 목장 거리·광장·마을회관·기도정원·함께하는 사역 |

부품은 `src/components/village/kit.tsx` 한 곳에, 데이터는 `src/db/village.ts` 한 곳에,
「내 목장이 어디고 내가 목자인가」는 `src/hooks/use-cell-room.ts` 한 곳에 있다.

---

## 정한 것과 이유

**공개 범위를 칸이 아니라 표로 갈랐다.** 기획서 §5 는 글마다 여섯 단계를 고르게 하지만,
RLS 는 칸을 못 지킨다. 한 표에 민감한 것과 공개할 것을 같이 담고 `visibility` 칸으로
가르면 그 칸을 잘못 쓴 순간 병·가정사가 통째로 열린다. 그래서

- 목장 안에서 서로 보는 것 → `cell_shares` · `cell_gatherings` · `cell_ministries` …
- 담당자만 보는 것 → `cell_nurtures.note` · `cell_care_notes`
- 마을로 나가는 것 → `village_*` (승인을 거쳐 **복사**되어 올라간다)

목장 글이 마을로 가는 길은 한 방향이다: **제안 → 마을장 승인 → 광장**.
마을 화면이 목장 표를 직접 읽지 않는다.

**양육은 숫자만 목장에 보인다.** 화면이 `cell_nurtures` 를 세지 않고
`cell_nurture_summary()` 가 돌려주는 집계만 쓴다 — 셀 수 있으면 읽을 수 있고,
읽을 수 있으면 언젠가 이름이 샌다. 이름이 나가는 것은 본인이 적은 `celebrate`(축하
소식) 한 칸뿐이다. 점수·순위·경고 배지는 두지 않았다(기획서 §2).

**돌봄실은 목자·교역자만 연다.** 「그 목장 사람이면」이 아니다. 돌봄 대상은
`members`(교적)로 가리킨다 — 앱을 안 쓰는 성도도 돌봄의 대상이라, 계정으로 가리키면
빠지는 사람이 생긴다.

**선교실에 계좌 칸이 없다.** 칸이 있으면 개인 계좌가 들어간다. 후원은 안내 문구로만
잇고 교회가 승인한 길로 보낸다(기획서 §2).

**모임 순서는 jsonb 한 칸이다.** 순서는 교회마다 다르고 모임마다 늘었다 줄었다 한다.
표를 따로 만들면 한 모임 만드는 데 여러 번 써야 하고, 화면은 어차피 통째로 읽어
통째로 쓴다. 검색할 일도 없다.

**참석은 답(`reply`)과 출석(`attended`)을 따로 담는다.** 「온다고 했는데 못 온 것」과
「안 온 것」은 목자에게 다른 일이다.

**기도 응답 시각은 트리거가 채운다.** 0076 에서 배운 그대로 — 화면이 채우면 기기
시계가 틀어진 만큼 기록이 틀어진다.

---

## 옛 목장방(`/r2m/cell`)과의 관계

0065·0068 로 만든 목장방은 **그대로 살아 있다**. 공지·보고·소통창·경건생활 상황표가
거기 있고, 이 화면들은 그 위에 「모임·나눔·양육·돌봄·사역·선교·마을」을 더한 것이다.
같은 표를 두 벌로 만들지 않았으므로 어느 쪽에서 적어도 어긋나지 않는다.

옮길지 말지는 나중 판단이다. 지금은 두 문이 다 열려 있다.

---

## 남은 일

- [ ] `0085_village_on.sql` 대시보드 실행 (이게 되어야 나머지가 뜬다)
- [ ] 교구(마을)를 실제로 나눌지 결정 — 안 나누면 새부대교회 11개 목장이 한 마을
- [ ] 마을장 지정(`org_units(district).leader_member_id`) — 비어 있으면 교역자가 승인
- [ ] 설교·주보 목장교재에서 모임 순서 초안 자동 생성 (기획서 §8 2차)
- [ ] 사진 올리기 (`photo_url` 칸은 표에 있으나 화면은 아직 안 씀)
- [ ] 알림 연결 — 새 모임·승인·기도 요청 ([[webpush-renotify-silent-replace]] 세 곳 같이 고칠 것)
