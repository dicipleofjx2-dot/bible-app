# 다음 창에서 이어받을 것 (2026-09-02)

두 앱을 같이 만진다. 같은 Supabase 프로젝트(`bhqbrkeoiyhnmdgvofvy`)를 쓴다.

- 데이빗바이블 `C:\Users\dicip\Documents\BibleApp` — 배포 https://dicipleofjx-bible.vercel.app
- 스마트주보 `C:\Users\dicip\Documents\dg-smart-bulletin` — 배포 https://dg-smart-bulletin.vercel.app

## 먼저 알아 둘 것

**SQL은 사람이 실행한다.** 이 환경에서 DDL을 돌릴 길이 없다(`exec_sql` 없음, DB 비밀번호 없음).
마이그레이션 파일을 만들어 사용자에게 보여 주면 대시보드 SQL 편집기에서 실행해 준다.
실행 여부는 REST로 함수·표를 찔러 확인한다.

**배포는 worktree로 격리한다.** 같은 리포를 다른 세션이 동시에 만지고 있어서
`vercel --prod`가 남의 미완성 코드를 끌어간다.

```bash
SP=<스크래치패드>
rm -rf "$SP/deploy" && git worktree prune && git worktree add --detach "$SP/deploy" HEAD
cp -r .vercel/. "$SP/deploy/.vercel/"   # 점(.)을 빠뜨리면 엉뚱한 새 프로젝트가 생긴다
cd "$SP/deploy" && npx vercel --prod --yes
```

**푸시하려면 GitHub 계정을 바꿔야 한다.** 활성 계정이 `bible663vision-lgtm`이라 403이 난다.

```bash
gh auth switch --user dicipleofjx2-dot
git push origin main        # BibleApp 은 main, 주보는 master
gh auth switch --user bible663vision-lgtm   # 반드시 되돌린다
```

## 지금 남은 일

### 1. 아직 실행 안 된 SQL 둘 (데이빗바이블)

| 파일 | 하는 일 |
|---|---|
| `0070_cell_names_include_writers.sql` | 목장방 글쓴이가 「이름 없음」으로 뜨던 것 |
| `0071_prayer_notify_team.sql` | 샬롬기도단 기도제목 → 기도단원에게만 알림 |

**0070** — 대표관리자(교적상 목장 없음)가 남의 목장방에 글을 쓰면 이름이 안 떴다.
`cell_member_names()`가 「그 목장 소속」만 돌려주기 때문. 「그 목장에 글을 쓴 사람」까지 넓혔다.
앱 코드는 손댈 것 없다 — 함수만 바뀐다.

**0071** — 실행 뒤 확인할 것:
- `push_outbox` 에 `topic='prayer'` 행이 쌓이는지 (기도제목 하나 올려 보고)
- 대상이 열 명인지 (`member_ministries` → 샬롬기도팀)
- 잠금화면에 **내용은 안 실린다** — 「○○○ 님이 기도를 부탁하셨습니다」까지만

### 2. 출석 초청을 R2M 훈련 신청자에게 — **코드는 이미 다 있다**

`{ kind: "training", courseIds }` 대상이 구현돼 있고(`lib/queries/gather.ts:162`),
화면도 훈련과정 목록을 받는다. 신청자도 39명 들어 있다:

```
R2M 1단계 6명 · R2M 2단계 28명 · 성경통독 일년일독 5명
```

**막는 것은 설정 하나** — 훈련과정 5개가 전부 `is_active = false` 다.
`getTrainingCourses(churchId, includeInactive=false)` 가 켜진 것만 목록에 넣으므로
대상 고르기에 훈련이 안 뜬다. 주보 교적 → 훈련 관리에서 과정을 켜면 바로 된다.

⚠️ 켜면 성도 화면에도 그 과정이 보인다. 사용자에게 물어보고 켤 것.

### 3. 목회AI 「오늘의 목양」 기도제목에 답글 (아직 설계 안 함)

사용자 요청: *"목회AI에서 오늘의 목양에 기도제목이 뜨면, 그 기도에 답글을 달아주고,
그 기도제목을 올린 사람이 볼 수 있게 해주면 어떨까?"*

**먼저 확인해야 할 것**: 「오늘의 목양」에 뜨는 기도제목이 어느 표에서 오는가.
후보가 둘이고 성격이 아주 다르다.

- `care_records(record_type='prayer')` — 주보. 목회자가 심방하며 **적어 두는** 기록.
  올린 사람이 따로 없으므로 "올린 사람이 본다"가 성립하지 않는다.
- `prayer_requests` — 데이빗바이블 샬롬기도단. 성도가 **직접 올린** 것.
  이쪽이면 `prayer_comments` 가 이미 있다(0015). 답글 기능이 이미 있는 셈이라
  「목회AI 화면에서 그 댓글을 달 수 있게」가 실제 할 일이 된다.

세 번째 가능성: 주보 목양 화면이 아직 `prayer_requests` 를 못 읽는다.
두 앱이 같은 DB라 읽기만 열면 된다 — 다만 성도가 **서로 나누려고** 쓴 글을
목회 기록으로 끌어오는 것이라, 그대로 옮기지 말고 읽기만 할지 사용자와 정할 것.

## 최근에 고친 것들 (되돌아볼 때 참고)

- **광고가 주보마다 따로가 아니었다** (주보 `0074`, 커밋 `37bdb19`).
  `announcements.bulletin_id` 칸은 있었는데 조회가 `church_id` 로만 걸렀고 만들 때도 안 채웠다.
  같은 모양의 면이 셋 더 있다 — **주간 일정 · 교회 캘린더 · 모임(Gather)**.
  지난 주보를 열면 지금 것이 보인다. 사용자에게 물어봐야 한다(캘린더는 "지금"이 맞을 수도).
- **헌금 항목**을 통장 적요에서 가려낸다(주보 `0073`). 「항목 확인 필요」 1136건이 남아 있고
  사람이 「확인」을 눌러 줄여 나가는 중.
- **점수 셈이 두 군데** 있어 큐티 30점이 순위표에만 반영됐다(`cd4ee36`).
  `db.ts` 와 DB 함수가 **같은 숫자**여야 한다 — 0050 주석이 그렇게 경고한다.
- **화면과 서버에 같은 규칙**을 둔 곳이 자꾸 나온다. 예배순서 면을 여러 개 넣게 할 때
  화면만 고쳐 "그 면은 이미 있습니다"가 떴다(`1adf08c` → `0448027`).

## 손이 필요한 것 (코드 아님)

1. **목장 정기모임 등록** — 12개 목장 다 비어 있어 목장방 「이번 주 모임」에 안내 문구만 뜬다.
   주보 「모임 관리」에서 `category='cell'`, `org_unit_id`=목장으로 넣으면 교회 캘린더에도 함께 뜬다.
2. **헌금 항목 확인 1136건**
3. **목장 없는 교인 11명** — 앱 쓰는 분은 김치훈(대표관리자 본인)·박민지 둘.
   김치훈 님이 목장이 없어서 0070 버그가 드러났다.
