# 다음 창에서 이어받을 것 (2026-09-02)

두 앱을 같이 만진다. 같은 Supabase 프로젝트(`bhqbrkeoiyhnmdgvofvy`)를 쓴다.

- 데이빗바이블 `C:\Users\dicip\Documents\BibleApp` — 배포 https://dicipleofjx-bible.vercel.app
- 스마트주보 `C:\Users\dicip\Documents\dg-smart-bulletin` — 배포 https://dg-smart-bulletin.vercel.app

## 먼저 알아 둘 것

**SQL은 사람이 실행한다.** 이 환경에서 DDL을 돌릴 길이 없다(`exec_sql` 없음, DB 비밀번호 없음).
마이그레이션 파일을 만들어 사용자에게 보여 주면 대시보드 SQL 편집기에서 실행해 준다.
실행 여부는 REST로 함수·표를 찔러 확인한다.

**배포는 worktree로 격리한다.** 같은 리포를 다른 세션이 동시에 만지고 있어서
`vercel --prod`가 남의 미완성 코드를 끌어간다.

```bash
SP=<스크래치패드>
rm -rf "$SP/deploy" && git worktree prune && git worktree add --detach "$SP/deploy" HEAD
cp -r .vercel/. "$SP/deploy/.vercel/"   # 점(.)을 빠뜨리면 엉뚱한 새 프로젝트가 생긴다
cd "$SP/deploy" && npx vercel --prod --yes
```

**푸시하려면 GitHub 계정을 바꿔야 한다.** 활성 계정이 `bible663vision-lgtm`이라 403이 난다.

```bash
gh auth switch --user dicipleofjx2-dot
git push origin main        # BibleApp 은 main, 주보는 master
gh auth switch --user bible663vision-lgtm   # 반드시 되돌린다
```

## 휴대폰(클로드 앱)에서 이어 하기

두 앱 다 깃허브에 있다. 클로드 앱에서 저장소를 고르면 이 작업을 그대로 이어받는다.

| 앱 | 저장소 | 가지 |
|---|---|---|
| 데이빗바이블 | `dicipleofjx2-dot/bible-app` | `main` |
| 데이빗 큐티 | `dicipleofjx2-dot/dg-qt` | `main` |

- 둘 다 **비공개**다. 클로드 앱에서 안 보이면 깃허브 연결에 그 저장소 권한을 열어 준다.
- 휴대폰에서 고친 것은 깃허브로 들어오므로, PC 에서 이어 할 때 **`git pull` 을 먼저** 한다.
- **비밀 키는 저장소에 없다**(`.env*` 는 올리지 않는다). 휴대폰에서 배포하거나 DB 를
  건드리는 일은 못 한다 — 코드 고치기·읽기까지다.
- 스마트주보(`dg-smart-bulletin`)는 아직 이 정리를 안 했다.

## 지금 남은 일

### 1. 아직 실행 안 된 SQL 둘 (데이빗바이블)

| 파일 | 하는 일 |
|---|---|
| `0070_cell_names_include_writers.sql` | 목장방 글쓴이가 「이름 없음」으로 뜨던 것 |
| `0071_prayer_notify_team.sql` | 샬롬기도단 기도제목 → 기도단원에게만 알림 |
| `0072_gratitude_journal.sql` | 감사일기장 표·사진 통·「함께 나누기」 (안 돌리면 감사 화면이 빈다) |

큐티 앱(`Documents/dg-qt/supabase/migrations/`)에도 둘 남았다 — 같은 프로젝트라 같은 SQL 편집기에서 돌린다.

| 파일 | 하는 일 |
|---|---|
| `0004_qt_paragraphs.sql` | 본문 문단 나눔 저장 칸 (안 돌리면 문단이 그 기기에만 남는다) |
| `0005_qt_checks_r2m.sql` | 큐티 30점 → R2M 「오늘의 훈련」 QT 자동 체크 |

**0005 는 지난 38일치를 채우는 것까지 이미 REST 로 해 뒀다.** SQL 은 앞으로 저장되는 큐티에
트리거를 걸기 위해 필요하다 — 안 돌리면 오늘부터 다시 손으로 눌러야 한다.

**0070** — 대표관리자(교적상 목장 없음)가 남의 목장방에 글을 쓰면 이름이 안 떴다.
`cell_member_names()`가 「그 목장 소속」만 돌려주기 때문. 「그 목장에 글을 쓴 사람」까지 넓혔다.
앱 코드는 손댈 것 없다 — 함수만 바뀐다.

**0071** — 실행 뒤 확인할 것:
- `push_outbox` 에 `topic='prayer'` 행이 쌓이는지 (기도제목 하나 올려 보고)
- 대상이 열 명인지 (`member_ministries` → 샬롬기도팀)
- 잠금화면에 **내용은 안 실린다** — 「○○○ 님이 기도를 부탁하셨습니다」까지만

### 2. 출석 초청을 R2M 훈련 신청자에게 — **끝났다(2026-09-02)**

훈련과정 5개를 모두 켰다(`training_courses.is_active = true`, 사용자 승인).
이제 대상 고르기에 훈련이 뜨고 신청자 39명에게 초청을 보낼 수 있다.
**성도 화면에도 다섯 과정이 보인다** — 이게 싫으면 주보 「훈련 관리」에서 다시 끈다.

아래는 그때의 조사 내용(참고용).

#### (지난 기록) 코드는 이미 다 있다

`{ kind: "training", courseIds }` 대상이 구현돼 있고(`lib/queries/gather.ts:162`),
화면도 훈련과정 목록을 받는다. 신청자도 39명 들어 있다:

```
R2M 1단계 6명 · R2M 2단계 28명 · 성경통독 일년일독 5명
```

**막는 것은 설정 하나** — 훈련과정 5개가 전부 `is_active = false` 다.
`getTrainingCourses(churchId, includeInactive=false)` 가 켜진 것만 목록에 넣으므로
대상 고르기에 훈련이 안 뜬다. 주보 교적 → 훈련 관리에서 과정을 켜면 바로 된다.

⚠️ 켜면 성도 화면에도 그 과정이 보인다. 사용자에게 물어보고 켤 것.

### 3. 목회AI 「오늘의 목양」 기도제목에 답글 (아직 설계 안 함)

사용자 요청: *"목회AI에서 오늘의 목양에 기도제목이 뜨면, 그 기도에 답글을 달아주고,
그 기도제목을 올린 사람이 볼 수 있게 해주면 어떨까?"*

**먼저 확인해야 할 것**: 「오늘의 목양」에 뜨는 기도제목이 어느 표에서 오는가.
후보가 둘이고 성격이 아주 다르다.

- `care_records(record_type='prayer')` — 주보. 목회자가 심방하며 **적어 두는** 기록.
  올린 사람이 따로 없으므로 "올린 사람이 본다"가 성립하지 않는다.
- `prayer_requests` — 데이빗바이블 샬롬기도단. 성도가 **직접 올린** 것.
  이쪽이면 `prayer_comments` 가 이미 있다(0015). 답글 기능이 이미 있는 셈이라
  「목회AI 화면에서 그 댓글을 달 수 있게」가 실제 할 일이 된다.

세 번째 가능성: 주보 목양 화면이 아직 `prayer_requests` 를 못 읽는다.
두 앱이 같은 DB라 읽기만 열면 된다 — 다만 성도가 **서로 나누려고** 쓴 글을
목회 기록으로 끌어오는 것이라, 그대로 옮기지 말고 읽기만 할지 사용자와 정할 것.

### 4. 중복 계정 정리 — **1차 끝났다(2026-09-02)**

한 사람이 계정을 둘·셋씩 갖고 있어 통독 기록과 포인트가 갈라져 있었다.
18묶음(계정 25개)을 **카카오 계정 하나로** 합쳤다 — 217행을 옮기고 26행은 겹쳐서 버렸다.
마지막 하나는 엄세미 님(`ssem0514@naver.com` → `sm6love@naver.com`)인데, **둘 다 카카오**이고
이메일도 이름도 안 겹쳐 기계로는 못 찾았다 — 사용자가 알려 줘서 합쳤다.
도구는 `scripts/merge-accounts.mjs`, 짝은 `scripts/merge-accounts.plan.json`(git 에 안 올림,
사본은 `_supabase-backup/merge-accounts.plan-2026-09-02.json`).
**되돌리기 자료**: `_supabase-backup/merge-accounts-rollback-*.json`.
⚠️ 처음 17묶음(213행)치는 **잃었다** — 파일 이름에 날짜만 붙여 둬서 엄세미 건을 두 번째로
돌릴 때 30행짜리가 덮어썼다. 스크립트는 고쳤다(이름에 시각까지 + 있으면 번호). 짝은 사용자가
확인했고 옛 계정도 안 지웠으니 되돌릴 일은 없을 것으로 본다.

짝은 ① 이메일 아이디 ② 교적 전화번호 ③ 카카오 이름 세 가지로 찾았다.
전화번호가 결정적이었다 — `sk01058080850@gmail.com` 은 홍수빈 님 전화번호 그대로였고,
`jaeyeon7151`·`cat6956`·`pej8588` 도 전화 뒷자리로 확인됐다. **이름만 보고는 못 찾는다** —
`gimhuiwon647`(기록 36행)은 짝의 카카오 별명이 `rlagmldnjs`(=김희원을 영문 자판으로 친 것)라
겨우 알아봤다.

**안 합친 것**: `yooksk2010`(강지원) / `yooksk2016`(강지은) — 아이디는 한 글자 차이지만
교적 이름이 달라 **다른 사람**이다(사용자 확인). 짝을 못 찾은 계정도 남아 있다 —
`ssem0514`(30행)·`happytown69`(11)·`gracetograce618`(4)·`kihlhj6390`(4) 등은
교적에도 안 붙어 있어 누구인지 알 길이 없다. 본인이 「예전 계정 잇기」(0067)로 잇는 수밖에 없다.

**옛 계정은 지우지 않았다.** 기록만 옮겨 비어 있다. 외래키 때문에 auth 사용자 삭제가
조용히 막히기도 하고, 잘못 합쳤을 때 되돌릴 자리를 남겨 둔 것이다.

### 5. 큐티 — 본문을 화면 안에서 읽고 문단을 나눈다 (2026-09-02, dg-qt)

「오늘의 큐티」는 `USE_QT_APP = true` 라 데이빗 큐티(`Documents\dg-qt`)가 연다.
거기서 본문을 바깥 사이트 링크 대신 **개역개정 전문**으로 펼치게 하고, 절 사이를 눌러
문단을 나누는 기능을 넣었다. 배포됨(https://dg-qt.vercel.app).
**저장소를 새로 만들었다** — https://github.com/dicipleofjx2-dot/dg-qt (비공개).
남은 것: `supabase/migrations/0004_qt_paragraphs.sql` 실행(안 돌리면 문단이 기기에만 남는다).

### 6. 감사일기장 · 통독 체크 기준 (2026-09-02)

**감사일기장** — 감사 기록이 기기 안 SQLite 에만 있어 휴대폰을 바꾸면 사라졌고 사진도 못 넣었다.
`0072` 로 서버에 옮기고 화면을 일기장처럼 다시 짰다(날짜 리본·사진·감사 세 줄·긴 메모).
**기본은 나만 본다** — 글마다 「함께 나누기」를 켠 것만 「함께 나눈 감사」에 뜬다.
기기에만 있던 예전 감사는 화면을 처음 열 때 한 번 올라간다(`syncLocalGratitudeOnce`).
시안: https://claude.ai/code/artifact/652d932e-f5eb-4483-9645-968424e11c37

⚠️ **로그인해야 쓸 수 있게 됐다.** 예전에는 로그인 없이도 기기에 적을 수 있었다.
서버에 담으면서 그 길이 막혔다 — 카카오 로그인만 있는 앱이라 괜찮다고 봤지만, 문제가 되면 알려 줄 것.

**통독 체크 기준을 70 → 80점으로** 올렸다(`READING_QUIZ_PASS_SCORE`). DB 는 이미 80을 쓰고 있었다
(함께읽기 0044·알림 0045·현황판 0047·순위표 0046·0049). 여기만 70이라 70~79점인 날은 경건훈련엔
체크가 서는데 통독 현황판엔 「안 한 날」로 잡혔다. **80점을 못 넘던 분들은 이제 체크가 안 선다** —
너무 빡빡하면 이 상수 하나만 되돌리면 된다.

## 최근에 고친 것들 (되돌아볼 때 참고)

- **광고가 주보마다 따로가 아니었다** (주보 `0074`, 커밋 `37bdb19`).
  `announcements.bulletin_id` 칸은 있었는데 조회가 `church_id` 로만 걸렀고 만들 때도 안 채웠다.
  같은 모양의 면이 셋 더 있다 — **주간 일정 · 교회 캘린더 · 모임(Gather)**.
  지난 주보를 열면 지금 것이 보인다. 사용자에게 물어봐야 한다(캘린더는 "지금"이 맞을 수도).
- **헌금 항목**을 통장 적요에서 가려낸다(주보 `0073`). 「항목 확인 필요」 1136건이 남아 있고
  사람이 「확인」을 눌러 줄여 나가는 중.
- **점수 셈이 두 군데** 있어 큐티 30점이 순위표에만 반영됐다(`cd4ee36`).
  `db.ts` 와 DB 함수가 **같은 숫자**여야 한다 — 0050 주석이 그렇게 경고한다.
- **화면과 서버에 같은 규칙**을 둔 곳이 자꾸 나온다. 예배순서 면을 여러 개 넣게 할 때
  화면만 고쳐 "그 면은 이미 있습니다"가 떴다(`1adf08c` → `0448027`).

## 손이 필요한 것 (코드 아님)

1. **목장 정기모임 등록** — 12개 목장 다 비어 있어 목장방 「이번 주 모임」에 안내 문구만 뜬다.
   주보 「모임 관리」에서 `category='cell'`, `org_unit_id`=목장으로 넣으면 교회 캘린더에도 함께 뜬다.
2. **헌금 항목 확인 1136건**
3. **목장 없는 교인 11명** — 앱 쓰는 분은 김치훈(대표관리자 본인)·박민지 둘.
   김치훈 님이 목장이 없어서 0070 버그가 드러났다.
