/**
 * Supabase Storage 에 올리는 파일의 캐시 수명.
 *
 * **이 상수가 없어서 2026-08-29 에 프로젝트 전체가 잠겼다.** 업로드할 때
 * cacheControl 을 안 주면 Supabase 는 캐시 수명을 **1시간**으로 잡는다. 그래서
 * 같은 전자책·같은 목자편지 사진을 사람들이 한 시간마다 처음부터 다시 받았고,
 * 창고에 쌓인 파일은 0.6GB 인데 **밖으로 나간 양이 15GB** 가 되어 무료 한도
 * (5GB)를 세 배로 넘겼다. 데이터가 많아서가 아니라 같은 것을 스물네 번씩
 * 다시 보내서 터진 것이다.
 *
 * 1년으로 잡아도 안전한 이유: 이 앱은 올릴 때마다 파일 이름을
 * `${Date.now()}-${랜덤}.pdf` 로 새로 짓고 **덮어쓰지 않는다.** 내용이 바뀌면
 * 주소도 바뀌므로, 옛 주소를 오래 캐시해 둔다고 낡은 내용이 보일 일이 없다.
 *
 * ⚠️ 덮어쓰기(upsert)를 하는 업로드가 생기면 이 상수를 그대로 쓰면 안 된다.
 *    같은 주소에 새 내용을 넣는 순간 사람들은 1년 동안 옛것을 본다.
 */
export const STORAGE_CACHE_SECONDS = '31536000'; // 365일
